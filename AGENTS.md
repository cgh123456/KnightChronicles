# 项目协作与任务跟踪

## 必须维护的入口

项目根目录的 [TASK_STATUS.md](TASK_STATUS.md) 是任务计划、当前进度、待办事项和交接信息的统一入口。

1. 开始任务时，先阅读本文件、`TASK_STATUS.md` 以及涉及模块的说明。允许先做必要的只读检查；实质性实现、修改、下载或导入资产前，必须先在跟踪文档登记本次目标、范围、执行计划、验证方式与待办事项。
2. 执行过程中，在完成一个阶段、改变方案、遇到阻塞或暂停工作时更新进度，不要等到最终回复才补写。同步更新时间、当前正在做什么、下一步和相关文件。
3. 收尾时记录实际产物、验证结果、未完成项与限制。对话中的进度说明不能替代项目内记录。
4. 新任务追加独立编号，保留必要的历史记录；多人协作时注明负责方，不覆盖他人的任务状态。并行任务使用独立条目。

## 状态与真实性

- 使用明确状态：未开始、进行中、已完成、已交付待验收、阻塞、暂停。
- 区分“推荐方案”与“已采用方案”、“文件已生成”与“已验证”、“已交付”与“用户已验收”。
- 仅报告有依据的进度，不编造完成百分比；历史测试结果不得写成本次重测结果。
- 阻塞项说明原因、已尝试的办法及需要的输入；普通待办不自动视为阻塞。
- 超出本次请求的后续建议列为待办，不自动视为已获执行授权。

## 最小记录要求

每项任务至少包含：任务编号与目标、负责方、当前状态、计划步骤及勾选进度、产物路径、验证结果、待办或阻塞、最近更新时间。文档内使用仓库相对链接，方便其他协作者查看。

遵循用户最新要求；本文件不授权额外的项目变更。维护任务记录时保留现有工作，不顺带修改无关代码或资产。
